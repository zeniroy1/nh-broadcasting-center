# NH알리미 출근 진입 미발송 분석 리포트 v2

**작성일:** 2026-06-01
**버전:** v2 (소스 코드 교차 검증 반영)
**분석 대상 로그:** `NH알리미_런타임로그_20260601_전일1700-085345.txt` (682줄, 05-31 17:00 ~ 06-01 08:52)
**검증 소스:** `nh_reminder_v2_source_candidate_20260511`
**증상:** 2026-06-01 오전 출근 시 직장 도착했으나 진입(ENTER) 알림 미발송

> **v2 변경 요약:** 실제 소스 코드를 확인하여 v1의 일부 단정을 정정했다.
> - fresh 요청 = 정확도 10~20m 확보 → **보장 불가** (현재 fresh가 GPS가 아니라 NETWORK/medium)
> - 정밀위치 권한 꺼짐 → **단정에서 가설로 강등** (Manifest에는 정밀 권한 선언됨)
> - 오늘 정확도 100m의 1차 원인은 권한이 아니라 **provider 선택(NETWORK 우선)**일 가능성이 높음

---

## 1. 한 줄 결론

알림 기능·예약 기능의 고장이 **아니다.** service_only 저전력 상태에서 오래된 캐시 위치(stale lastKnown)가 우선 사용되어 실제 진입 순간의 신선한 위치를 확보하지 못했고, 그 캐시가 목적지 80m대에 고정된 채 진입 판정 반경(40m)을 끝내 충족하지 못한 것이 직접 원인이다. 부차적으로, 접근대기 로직이 보조가 아닌 **필수 게이트(AND 조건)**로 들어가 추가 실탐 누락 위험을 만든 상태다. 두 문제 모두 실제 코드에서 확인되었다.

---

## 2. 시스템 동작 전제 (로그 + 코드 확인)

| 항목 | 값 | 근거 |
|---|---|---|
| 지오펜스 중심 | lat 37.566, lng 126.9673 | 로그 |
| 진입 판정 반경 | **40m** | 로그 (출근/퇴근 사이클 분리용, 4장 참조) |
| 진입(통과) 기준 | 중심권 20m 이내 **또는** 정확도 40m 이하 | 로그 |
| 이탈 기준 | 55m 초과 | 로그 |
| 평상시 감시 간격 | 120초 (service_only 저전력) | 로그 |
| 위치 소스 | lastKnown(캐시) 우선 | `background_monitor_service.dart` L355~377 |

크래시·스케줄 누락은 없었다. 진단 요약 전 구간 `실패/timeout:0회`, `실행지연:0회`로 코드 자체는 정상 동작했다.

---

## 3. 오늘 아침 실제 이동 궤적 (로그 발췌)

| 시각 | 거리 | 정확도 | 위치 소스 | 판정 |
|---|---|---|---|---|
| 08:21:28 | 10,426m | 100m | lastKnown | 보류 |
| 08:31:29 | 5,241m | 43m | lastKnown | 보류 |
| 08:33:29 | 3,476m | 24m | lastKnown | 보류 |
| 08:37:29 | 1,629m | 85m | lastKnown | 보류 |
| 08:39~08:45 | **1,637m 고정** | 108m | lastKnown (age 479초까지 증가) | 보류 |
| 08:47:07 | **125m** | 77m | lastKnown | 보류 |
| 08:48:07 | 85m | 93m | 재캐시 | 보류 |
| 08:48~08:52 | **81~85m 고정** | 93~100m | lastKnown | **ENTER 대기 지속** |

목적지 약 80m 지점까지 도달했으나 끝까지 "범위 밖, ENTER 대기"에 머물렀다.

---

## 4. 직접 원인 분석 (코드 교차 검증)

### 4-1. 저전력 상태에서 캐시가 있으면 즉시 반환 — 핵심 원인

`background_monitor_service.dart` L355~377:

```dart
if (dismissedLowPower || notifActive || inactiveLowPower) {
  final lastKnownJudgment = await _judgmentFromLastKnownForLowPower(...);
  if (lastKnownJudgment != null) {
    await _incrementDiagnosticCounter(prefs, _diagLastKnownCountKey);
    return lastKnownJudgment;        // ← 캐시 있으면 여기서 즉시 종료
  }
}
// 아래 fresh 요청까지 도달하지 못함
return LocationJudgmentService.fromCurrentPosition(...);
```

service_only(inactiveLowPower) 상태에서 사용 가능한 캐시가 있으면 fresh 요청 경로(L380~387)에 **도달조차 하지 않는다.** 진단 요약의 `위치요청:0회 / lastKnown:8회` 반복이 이를 그대로 보여준다. 08:39~08:45 구간 거리값이 1,637m로 6분간 고정되고 age가 479초까지 증가한 것이 stale 캐시 재사용의 명백한 정황이다.

### 4-2. fresh를 떠도 GPS가 아니라 NETWORK/medium — v1 정정

설령 fresh 경로에 도달해도, 현재 코드는 고정밀이 아니다.

**네이티브** (`NhBackgroundService.kt` L855~860):

```kotlin
private fun selectProvider(locationManager: LocationManager): String? {
    return when {
        locationManager.isProviderEnabled(NETWORK_PROVIDER) -> NETWORK_PROVIDER  // ← 우선
        locationManager.isProviderEnabled(PASSIVE_PROVIDER) -> PASSIVE_PROVIDER
        else -> null
    }
}
```

`GPS_PROVIDER`는 후보에 없다. lastKnown 수집(L840~841)도 `NETWORK_PROVIDER`, `PASSIVE_PROVIDER`만 본다.

**Flutter** (`background_monitor_service.dart` L385):

```dart
desiredAccuracy: geo.LocationAccuracy.medium,   // high/best 아님
timeout: const Duration(seconds: 15),
```

→ 따라서 **"fresh 1회 = 정확도 10~20m 확보"는 현재 코드에서 보장되지 않는다.** fresh를 떠도 NETWORK/medium이라 정확도가 수십~100m로 나올 수 있다. 오늘 정확도 100m의 가장 유력한 설명은 권한 문제가 아니라 **이 provider 선택**이다.

### 4-3. 반경(40m)보다 위치 오차(80~100m)가 더 큼 — 구조적 충돌

판정 단위(40m)보다 측정 오차(80~100m)가 크면 어떤 알고리즘으로도 40m 안쪽을 신뢰성 있게 판정할 수 없다. 정확도 15~20m급 측정이 들어와야 풀린다. 그런데 4-2에 따라 현재 코드는 그런 측정을 만들어내는 경로가 없다.

### 4-4. 도착 순간 다시 백그라운드 전환

08:47:55 앱이 잠깐 포그라운드로 떠 85~95m 측정을 얻었으나 여전히 40m 밖이었고, 08:48:32 `activity_destroy`로 다시 저전력 캐시 모드로 복귀했다. 정밀 측정으로 좁히기 전에 절전으로 돌아갔다.

### 4-5. 정밀위치 권한 — 가설로 강등 (v1 정정)

`AndroidManifest.xml` L6~7에 `ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION`이 **둘 다 선언**되어 있다. 매니페스트상 정밀 권한은 존재한다. 사용자가 런타임에 "근사 위치만" 허용했는지는 로그만으로 단정할 수 없으므로 **확인 대상 가설**로 둔다. 단, 4-2 때문에 권한이 정밀이어도 현재 코드 경로로는 GPS를 쓰지 않아 정확도 100m가 나올 수 있다는 점에 유의한다.

---

## 5. 보조 게이트 로직 검토 (접근대기) — 코드 확인

### 5-1. 접근대기가 필수 게이트(AND)로 들어감 — Dart·Native 양쪽 동일

**Flutter** (`background_monitor_service.dart` L153~156):

```dart
final initialAlertCandidate = !dismissedUntilExit &&
    !notifActive &&
    judgment.isInitialAlertCandidate &&
    (judgment.isStrongInitialAlertCandidate || approachPendingActive);  // ← AND 게이트
```

**Native** (`NhBackgroundService.kt` L918~921):

```kotlin
val initialAlertCandidate = !config.dismissedUntilExit &&
    !config.notifActive &&
    judgment.initialAlertCandidate &&
    (judgment.strongInitialAlertCandidate || approachPendingActive)  // ← 동일 구조
```

strong 후보가 아니면 `approachPendingActive`가 참이어야만 통과한다. 즉 접근대기가 일반 초기 알림의 **필수 조건**이 됐다. 의도(보조 보강)와 다르다. **두 파일에 같은 결함이 대칭으로 존재**하므로, Flutter만 고치면 앱을 닫은 service_only 상태에서 개선 효과가 약해진다.

### 5-2. 비대칭 원칙 위반

> 보조 신호는 **실탐을 늘리는 데(OR 보강)**만 써야 하고, **실탐을 막는 데(AND 게이트)** 써서는 안 된다.

접근대기가 비는 상황 = service_only 저전력에서 lastKnown만 쓰는 상황 = 오늘 아침 상황이다. 추세를 못 따라가는 모드에서 추세를 필수 조건으로 요구하니 필요한 순간에 정확히 무력화됐다.

### 5-3. 단, 게이트 완화만으로는 오늘이 안 풀린다

오늘 최선값 81~85m는 게이트 이전의 60m 기준조차 통과 못 한다. **두 문제는 독립이며 둘 다 고쳐야 한다.**

| 문제 | 원인 | 해결 |
|---|---|---|
| 오늘 미발송 (실탐 누락) | stale lastKnown + fresh가 NETWORK/medium | 근접 시 캐시 중단 + **GPS/high fresh** 요청 |
| 게이트 잠재 위험 | 접근대기 AND 필수화 (Dart·Native) | OR 보조로 강등 (양쪽) |

### 5-4. 기존 통과 경로가 오늘을 메꿀 수 있는가 → 불가

reliableInside(40m), strong(50m + 정확도 80m) 모두 "좋은 값이 들어왔다"는 전제에서만 작동한다. 오늘 최선값(81~85m / 93~100m)은 둘 다 탈락. 경로가 무능한 게 아니라 **양질의 입력을 만들어 줄 단계가 없어 굶고 있는 상태**다. 그 입력을 만들려면 4-2의 provider/정확도 승격이 선행돼야 한다.

---

## 6. 반경 40m 제약 — 줄여서는 안 되는 값

> **v1 정정:** 초기 "반경 100~150m 확대" 제안은 아래 제약을 반영해 **철회한다.**

지오펜스 중심과 실제 직장의 거리가 약 100m다. 반경 40m는 정확도 때문이 아니라 **출근 진입권과 퇴근 대기권을 분리하기 위한 의도적 설계**다.

- 반경을 키우면 → 출근 완료 상태가 이미 진입권 안 → 이탈→재진입 사이클 성립 불가 → 다음 출근 진입 감지 불가

따라서 반경은 유지하며, 정밀도 확보의 모든 부담이 fresh 측정 품질에 쏠린다. 반경을 못 키우는 이상 **"근접 시 고정밀 fresh 1회"가 유일한 해법**이다.

---

## 7. 권장 수정 방향

### 7-1. 알림 판정 구조 복원 (접근대기 강등) — Dart·Native 양쪽

접근대기를 알림을 막는 AND 조건에서 제거하고, 신뢰도를 더하는 보조 데이터로만 사용한다.

```
reliableInside : 그대로 유지
초기 알림 후보 : 그대로 유지 (60m 상한 유지)
strong 후보    : 빠른 통과 유지
접근대기       : 필수 게이트(AND) → 보조 보강(OR)으로 강등   ← Dart L153, Native L918
추세 보정      : 다음 버전 후보
```

단, 접근대기를 **왜 넣었는지(원래 60m 로직의 오탐 억제 목적인지)** 먼저 확인한다. 단순 강등 시 그 오탐이 되돌아오므로, fresh 재확인이 그 자리를 메워야 안전하다.

### 7-2. 평상시 절전 유지

`background_recent`, `service_only`에서는 기존처럼 120초 + lastKnown 중심으로 동작시킨다. 상시 고정밀 GPS는 켜지 않는다.

### 7-3. 근접 시에만 캐시 우선 중단 + 고정밀 단발 fresh

```
평상시        : 120초 lastKnown 저전력 유지
200~300m 접근 : 캐시 우선 잠시 중단, 단발 fresh 요청 (정밀 모드 진입)
60~80m 접근권 : 제한된 횟수만 20~30초 간격 재확인
40m 이내      : 진입 알림 판정
```

> **중요 (v2 핵심 보강):** 이 구간의 fresh 요청은 반드시 **provider를 GPS로, desiredAccuracy를 high/best로 승격**해야 한다. 현재처럼 NETWORK/medium(4-2)이면 fresh를 떠도 40m 판정에 필요한 정밀도가 나오지 않아 효과가 반감된다. Native `selectProvider()`(L855)와 Flutter `desiredAccuracy`(L385) 모두 근접 구간에서는 고정밀로 분기해야 한다.

**오늘 데이터 검증:** 08:47(125m) 시점에 이미 트리거가 걸렸어야 한다. 그때 GPS/high fresh를 떴다면 정확도 10~20m 값이 들어와 경로 1·2가 자연스럽게 통과시켰을 것이다. 직장까지 100m로 짧으므로 트리거를 200~300m로 앞당겨 40m권 도달 전에 정밀 모드를 확보하는 것이 안전하다.

### 7-4. fresh 요청 안전장치 (필수)

배터리 절전 효과를 유지하려면 반드시 함께 적용한다.

- **timeout 10~15초** (현재 Flutter는 15초 적용됨 — 유지)
- **lastKnown fallback** — fresh 실패 시 캐시로 진행
- **재시도 횟수 제한** — 무한 재측정 방지
- **재발동 cooldown** — 거리값이 정확도 100m로 출렁여(1,635↔1,637m) 트리거를 들락날락하며 반복 요청하는 것 방지

### 7-5. 네이티브 보조 감시도 동일 정책 적용

7-1·7-3을 Flutter에만 적용하면 앱을 닫은 service_only 상태에서 효과가 약하다. `NhBackgroundService.kt`의 게이트(L918)와 provider(L855)를 같은 정책으로 맞춘다.

### 7-6. 위치 권한 상태 로깅

Manifest에는 정밀 권한이 선언돼 있으나(L6), 사용자가 런타임에 근사 위치만 허용했는지는 별도 확인이 필요하다. **현재 권한 상태(fine/coarse, 백그라운드 허용 여부)를 진단 로그에 명시적으로 남겨** 이후 동일 증상 시 권한과 provider 요인을 구분할 수 있게 한다.

### 7-7. 이탈 판정 비대칭 — 현행 유지

진입 40m(빡빡) / 이탈 55m 초과(관대). 진입/이탈 떨림을 막는 좋은 설계이므로 유지한다.

---

## 8. 유지할 값 (변경 금지)

| 항목 | 값 |
|---|---|
| 감지 반경 | 40m |
| 이탈 기준 | 55m |
| 평상시 저전력 감시 | 120초 |
| 알림 시작 후 반복 알림 | 30초 |
| 알림 시작 후 위치 재확인 | 300초 |

**반경 확대·상시 고정밀 GPS는 권하지 않는다.** 이번 수정의 핵심은 알림 판정을 바꾸는 것이 아니라, **진입 직전에만 더 신선하고 정밀한 입력값을 공급하는 것**이다.

---

## 9. 알려진 한계

- **빠르게 지나가는 진입(짧은 체류)은 이 설계로도 놓칠 수 있다.** 120초 평상시 간격은 차량 통과 등에 구조적으로 늦다. 오늘 같은 "도착 후 머무름"은 잘 잡는다. 빠른 통과까지 잡으려면 접근권 간격 단축(20~30초, 7-3 반영)이 필요하다.
- **배터리 영향은 미미할 전망.** 오늘 약 13시간에 위치요청 1회. 접근권에서 1~3회 추가돼도 절전 구조에 큰 영향 없다.

---

## 10. 우선순위 요약

| 순위 | 항목 | 코드 위치 | 효과 |
|---|---|---|---|
| 1 | 근접 시 fresh를 **GPS/high로 승격** + 캐시 우선 중단 | `dart L355,L385` / `kt L855` | 오늘 미발송의 직접 해결 |
| 2 | fresh timeout·fallback·재시도 제한·cooldown | (신규) | 새 누락 방지·절전 유지 |
| 3 | 접근대기 게이트 AND → OR 강등 (Dart·Native) | `dart L153` / `kt L918` | 잠재 실탐 누락 제거 |
| 4 | 네이티브·Flutter 정책 일치 | 양쪽 | service_only에서 효과 확보 |
| 5 | 권한 상태 로깅 + 런타임 권한 확인 | `Manifest L6` | 권한/provider 요인 분리 |

**반경 40m, 이탈 55m, 120초, 반복 30초, 재확인 300초는 유지한다.**

---

*본 v2는 v1의 진단 골격(stale lastKnown + 접근대기 AND 게이트)을 유지하되, 실제 소스 코드 확인을 통해 (a) fresh가 NETWORK/medium이라는 점, (b) 정밀 권한은 Manifest에 선언돼 있다는 점을 반영하여 과한 단정을 정정하고, 수정안에 "fresh의 고정밀 승격"을 핵심으로 추가했다.*
